Description: >
    This cloudformation template deploys a VPC with both private and public 
    subnets splitted between 2 availability zones. It deploys an Internet Gateway with
    a default route for the private subnets. It deploys Bastion hosts, and 
    NAT Gateways within the public subnets, one per AZ. 

Parameters: